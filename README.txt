1. Install the packages using command 'pip install <package_name>' or 'pip3 install <package_name>'
The followings are the packages required:
	dash
	plotly
	pandas

2. Run the app by clicking on "app.py" or in cmd "python app.py"
3. Go to http://127.0.0.1:8050/ in Browser